# (reserved — see `../content/`)

Legal **source markdown** lives in [`../content/<product>/`](../content) (e.g. `content/driftora/`, `content/relo-dojo/`).
Built **public legal pages** are generated into [`../site/<repo>/`](../site) (`site/driftora/`, `site/relo_dojo/`).

This folder is an empty leftover from scaffolding (the Cowork sandbox can't delete dirs — see ORCHESTRAL_VIEW_PROMT §7); git won't track it once empty, and the owner can remove it locally.
